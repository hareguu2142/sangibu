<!doctype html>
<html lang="ko">
<head>
  <meta charset="UTF-8" />
  <title><%= title %></title>
  <link rel="stylesheet" href="/static/base.css" />
</head>
<body>
  <div class="wrap">
    <h1><%= title %></h1>
    <div class="tabs">
      <a href="/">① 입장/생성</a>
      <% if (typeof code !== 'undefined') { %>
        <a href="/c/<%= code %>?viewer=student">② 공유 리스트</a>
        <a href="/admin/<%= code %>">④ 관리자</a>
      <% } %>
    </div>
    <hr />
    <%- body %>
  </div>
</body>
</html>
